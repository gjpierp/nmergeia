# 后端弹性和容错能力

现代系统并不假设网络是值得信赖的。

##断路器
如果外部微服务持续失败，循环将通过返回快速错误而不是冻结执行线程来“打开”。

## 速率限制和节流
防止 DDOS 和滥用。使用 Redis 的*令牌桶*算法。

``美人鱼
图LR
  A[客户端] --> B[API 网关]
  B -->|慢速调用| C{断路器}
  C -->|打开| D[后备响应]
  C -->|关闭| E[皇家服务]
````

> [!NOTE]
> 白皮书的其余部分保留其原始语言，以保留代码和图表的语法。

